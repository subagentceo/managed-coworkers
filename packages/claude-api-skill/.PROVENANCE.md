# claude-api-skill — provenance

Imported 2026-05-18 from https://github.com/anthropics/skills/tree/main/skills/claude-api (default branch, sparse-checkout). Git history stripped before vendoring; only the `skills/claude-api/` subtree is in this package.

License: see `LICENSE.txt` (unchanged from upstream).

**Important divergence path:** the upstream SKILL.md teaches managed-agents usage assuming `ANTHROPIC_API_KEY`. This chassis is OAuth-only and is moving toward replay-only (no API key, no OAuth token). Future PRs in this repo will add a sibling SKILL or overlay that adapts the patterns here to the pollyjs-replay harness in REPLAY-1/-2/-3. Do NOT edit the upstream SKILL.md to bake in chassis-specific posture — keep upstream content faithful, add adaptations alongside.

To refresh:
```bash
git clone --depth 1 --filter=blob:none --sparse https://github.com/anthropics/skills.git
cd skills && git sparse-checkout set skills/claude-api
# replace this directory's contents with skills/claude-api/.
```
