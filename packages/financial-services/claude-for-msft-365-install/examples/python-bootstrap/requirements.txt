# Python bootstrap example dependencies.
# Versions pinned to security-clean floors (OSV-Scanner gate).
fastapi>=0.136.0
uvicorn>=0.30.0
PyJWT[crypto]>=2.12.0
# Transitive pins held to patched floors so `pip install -r` stays vuln-free:
starlette>=1.0.1
pydantic>=2.5.0
idna>=3.15
h11>=0.16.0
