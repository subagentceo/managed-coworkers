# financial-services — provenance

Imported 2026-05-18 from https://github.com/anthropics/financial-services-plugins (default branch, shallow clone). Git history and `.github/` directory stripped before vendoring; only the working tree is in this repo.

License: see `LICENSE` (unchanged from upstream).

To refresh: re-clone upstream, `rm -rf .git .github`, replace this directory, run `npm run verify`.
